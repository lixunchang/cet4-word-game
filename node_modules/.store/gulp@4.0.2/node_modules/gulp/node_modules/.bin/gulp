#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/../../../../../gulp-cli@2.3.0/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/../../../../../gulp-cli@2.3.0/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../gulp-cli@2.3.0/node_modules/gulp-cli/bin/gulp.js" "$@"
else
  exec node  "$basedir/../../../../../gulp-cli@2.3.0/node_modules/gulp-cli/bin/gulp.js" "$@"
fi
